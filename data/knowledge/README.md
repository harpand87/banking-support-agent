# Approved Knowledge Corpus

The current knowledge corpus is represented by the synthetic entries in `app/knowledge.py`. This folder is reserved for versioned source documents used by the embedding/RAG phase. Never add real customer records or secrets.
